[tool.cibuildwheel]
environment = { conda="$CONDA:/Users/69e/miniconda3/envs/syenv/bin" }
before-all = ["conda install conda-forge::libpng -y"]
build-verbosity = 1
build-frontend = "pip"
build = "cp310*"

[build-system]
requires = ["setuptools>=42", "wheel", "numpy>=1.21"]